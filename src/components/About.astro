---
import { Image } from "astro:assets";
import { about } from "../config/artist";
import { getSectionStyles } from "../utils/sectionStyle";

interface Props { defaultBg: string; }
const { defaultBg } = Astro.props;

const { style: sectionStyle } = getSectionStyles(about, defaultBg);
---

<section class="section" style={sectionStyle} id="about">
  <div class="section__inner about">
    <h2 class="section__heading">{about.heading}</h2>
    <div class="about__content">
      <Image
        src={about.image}
        alt={about.imageAlt}
        class="about__image"
        width={500}
        height={600}
        loading="lazy"
      />
      <div class="about__text" set:html={about.bio} />
    </div>
  </div>
</section>

<style>
  .about__content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }

  .about__image {
    width: 100%;
    height: auto;
    border-radius: 8px;
    object-fit: cover;
    aspect-ratio: 5 / 6;
  }

  .about__text {
    font-size: 1.05rem;
    line-height: 1.8;
    opacity: 0.85;
  }

  .about__text :global(p + p) {
    margin-top: 1.25rem;
  }

  @media (max-width: 768px) {
    .about__content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .about__image {
      max-width: 400px;
      margin: 0 auto;
    }
  }
</style>
